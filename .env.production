# In production builds, hit the real backend directly.
# The deployed backend must send CORS headers for your production origin.
VITE_API_BASE_URL=https://dev-co-production.up.railway.app/api/v1
# VITE_GOOGLE_MAPS_API_KEY=
